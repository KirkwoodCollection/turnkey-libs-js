// Main entry point - exports both core and React functionality

// Core utilities (framework-agnostic)
export * from './core';

// React-specific utilities (optional - only imported if React is available)
export * from './react';
